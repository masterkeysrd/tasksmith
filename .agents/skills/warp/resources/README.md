# Warp Resources

This directory contains specialized resources for working with Warp manifests.
You can populate this folder with:
- `schemas/`: JSON/YAML schemas for validation.
- `examples/`: Examples of `.warp` files, provider configurations, and tool mappings.
- `parsing.md`: Guidelines on how to use `internal/workspace` to read these files in Go.
